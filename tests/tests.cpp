// sim::Genome g;
// g.genome.emplace_back(0, 40, 0, 52, 0.0);
// g.genome.emplace_back(0, 14, 1, 6, 0.0);
// g.genome.emplace_back(0, 52, 0, 14, 0.0);
// g.genome.emplace_back(1, 1, 0, 40, 0.0);
// g.genome.emplace_back(0, 14, 0, 40, 0.0);
// ag.genome = g;
// ag.WireGenome();